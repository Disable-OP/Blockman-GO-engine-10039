{
  "name": "Blockman Engine Dev",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "hostRequirements": {
    "cpus": 4,
    "memory": "16gb",
    "storage": "32gb"
  },
  "runArgs": [
    "--privileged",
    "--device", "/dev/kvm"
  ],
  "features": {
    "ghcr.io/devcontainers/features/git:1": {},
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "postCreateCommand": "echo 'postCreateCommand works!' && ls -la .devcontainer/ && bash .devcontainer/setup.sh 2>&1 | tail -50; echo 'setup.sh done with exit code: '$?; bash .devcontainer/auto_build.sh 2>&1 | tail -50; echo 'auto_build.sh done with exit code: '$?",
  "forwardPorts": [5554, 5555, 19130]
}
